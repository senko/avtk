Local file support via FFmpeg
=============================

The :mod:`avtk.backends.ffmpeg` module implements inspection and manipulation of local audio and video files
using FFmpeg. In order to use this module, you'll need to have ``ffmpeg`` and ``ffprobe``
tools installed.

.. toctree::

   shortcuts
   convert
   probe
   cap
   modules

